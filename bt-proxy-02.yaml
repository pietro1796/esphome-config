# Pietro Customizations #
substitutions:
  device_name: bt-proxy-02
  friendly_name: BT-Proxy-02
  ip_addres: 192.168.100.52
  platform: esp32
  board: esp32-c3-devkitm-1

# COMMON CONFIG
packages:
  common: !include packages/common-espidf.yaml

###################
## DEVICE CONFIG ##
###################

# Bluetooth Configuration
esp32_ble_tracker:
  scan_parameters: 
    interval: 300ms
    window: 300ms

#  on_ble_advertise:
#    then:
#      - lambda: |-
#          ESP_LOGD("ble_adv", "BLE device %s RSSI %d", x.address_str().c_str(), x.get_rssi());

bluetooth_proxy:
  active: True